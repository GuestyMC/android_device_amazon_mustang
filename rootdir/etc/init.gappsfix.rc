# Disable GApps components that crash-loop before user unlock
# (crash loops during boot can trigger RescueParty data rollbacks)
on property:sys.boot_completed=1
    exec_background u:r:su:s0 root root -- /system/bin/sh -c "sleep 15 && cmd package disable-user com.google.android.tts"
